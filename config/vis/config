# $Arch: config,v 1.002 2017/08/07 20:23:53 kyau Exp $

# Currently available ones are "mpd", "pulse" and "alsa".
audio.sources=pulse
mpd.fifo.path=/tmp/mpd.fifo
# Defaults to spectrum,ellipse,lorenz
visualizers=spectrum
# Spectrum bar width. Defaults to 2.
visualizer.spectrum.bar.width=4
# Available smoothing options are monstercat, sgs, none.
visualizer.spectrum.smoothing.mode=monstercat
# Specifies the color scheme. (~/.config/vis/colors/)
colors.scheme=kyau

# vim: ft=conf ts=2 sw=2 noet :
